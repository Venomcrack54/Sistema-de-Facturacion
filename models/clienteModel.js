const ClienteModel = { obtenerClientes() { return JSON.parse(localStorage.getItem("clientes")) || []; }, guardarCliente(cliente) { const clientes = this.obtenerClientes(); clientes.push(cliente); localStorage.setItem("clientes", JSON.stringify(clientes)); return true; }, buscarPorCedula(cedula) { return this.obtenerClientes().find((c) => c.cedulaRUC === cedula); }, existe(cedula) { return this.obtenerClientes().some((c) => c.cedulaRUC === cedula); } };
